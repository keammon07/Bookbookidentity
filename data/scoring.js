const SCORE_KEYS=["builder","healer","explorer","detective","dreamwalker"];function createEmptyScores(){return SCORE_KEYS.reduce((a,k)=>(a[k]=0,a),{})}function addScore(target,points){Object.entries(points).forEach(([k,v])=>{if(k in target)target[k]+=Number(v)||0})}function normalizeScores(scores){const highest=Math.max(...Object.values(scores),1);return SCORE_KEYS.reduce((a,k)=>(a[k]=Math.round(scores[k]/highest*100),a),{})}function rankScores(scores){return Object.entries(scores).sort((a,b)=>b[1]-a[1]||SCORE_KEYS.indexOf(a[0])-SCORE_KEYS.indexOf(b[0])).map(([key,value])=>({key,value}))}
