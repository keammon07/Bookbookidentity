const DNA=(()=>{function render(container,percentages){container.innerHTML='';SCORE_KEYS.forEach(key=>{const c=CHARACTERS[key],row=document.createElement('div');row.className='dna-row';row.innerHTML=`<div class="dna-head"><span>${c.name.replace('THE ','')}</span><strong>${percentages[key]}%</strong></div><div class="dna-track"><div class="dna-bar" style="--dna-color:${c.color}" data-width="${percentages[key]}"></div></div>`;container.appendChild(row)});requestAnimationFrame(()=>requestAnimationFrame(()=>container.querySelectorAll('.dna-bar').forEach(bar=>bar.style.width=`${bar.dataset.width}%`)))}return{render}})();
